class ApiConfig {
  // Android emulator routes to host machine via 10.0.2.2
  // Change to your PC's local IP (e.g. 192.168.1.x) when using a physical phone
  static const String baseUrl = 'http://10.0.2.2:3000/api';
}
